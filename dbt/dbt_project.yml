name: iot_sensors
version: '1.0.0'
config-version: 2
profile: iot_sensors

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"

models:
  iot_sensors:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
